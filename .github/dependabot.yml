version: 2
updates:
- package-ecosystem: gradle
  directory: "/"
  schedule:
    interval: daily
  open-pull-requests-limit: 10
  ignore:
    - dependency-name: "spongeapi"
      # For Express, ignore all updates for version 4 and 5
      versions: ["8.x"]